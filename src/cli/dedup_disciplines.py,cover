  #!/usr/bin/env python3
> """Delete duplicate discipline: 'Operating Systems (Операционные системы)' — cascade deletes its competencies/links."""
! from __future__ import annotations
  
! import asyncio
! import sys
  
! sys.stdout.reconfigure(encoding="utf-8", errors="replace")
  
! from sqlalchemy import select
! from src.database import async_session_factory
! from src.models.krm_models import Direction, Discipline
  
  
! async def main():
!     async with async_session_factory() as session:
!         dir_result = await session.execute(
!             select(Direction).where(Direction.code == "09.03.02")
!         )
!         direction = dir_result.scalar_one_or_none()
!         if not direction:
!             print("Direction not found")
!             return
  
!         dup = await session.execute(
!             select(Discipline).where(
!                 Discipline.direction_id == direction.id,
!                 Discipline.name == "Operating Systems (Операционные системы)",
!             )
!         )
!         dup_disc = dup.scalar_one_or_none()
  
!         if not dup_disc:
!             print("No duplicate found")
!             return
  
!         await session.delete(dup_disc)
!         await session.commit()
!         print("Deleted duplicate: 'Operating Systems (Операционные системы)'")
  
  
! if __name__ == "__main__":
!     asyncio.run(main())
