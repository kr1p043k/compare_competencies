# Аудит: состояние проекта после улучшений .mimocode/

## Результат: качество НЕ улучшилось

Два плана в `.mimocode/plans/` содержат аудит (208 находок) и 8 конкретных фиксов для подсистемы рекомендаций. Бо́льшая часть фиксов **не была применена**.

---

## Статус 8 фиксов из eager-cactus.md

| # | Фикс | Статус | Доказательство |
|---|------|--------|----------------|
| 1 | GapAnalyzer type mismatch (rec_engine:153) | **Удалён мёртвый код** | `self.gap_analyzer` удалён, но тип не исправлен — просто убран |
| 2 | Readiness weights redesign (config.py:133) | **Частично** | Было 0.50/0.35/0.0/-0.15 (сумма 0.70). Proposed: 0.45/0.30/-0.25. Не применено |
| 3 | Readiness formula rewrite (profile_evaluator:266) | **НЕ применён** | Строка 276: `strong_count / market_div * 100` — старая формула, `*100` не убран |
| 4 | Wire readiness_weights param (profile_evaluator:38) | **НЕ применён** | Конструктор не принимает `readiness_weights` |
| 5 | Cap avg_gap to [0,1] (profile_evaluator:306) | **Применён** | `min(avg_gap, 1.0) * 100` — OK |
| 6 | cluster_relevance default (profile_evaluator:131) | **Применён** | `0.1 + 0.4 * (freq / max_weight)` вместо `0.15 * 0.0` — OK |
| 7 | Prophet degradation warning (prophet_forecast.py) | **НЕ применён** | Нет логирования fallback |
| 8 | Remove dead GapAnalyzer (rec_engine) | **Применён** | `self.gap_analyzer` удалён |

**Итого: 3 из 8 полностью применены, 1 частично, 4 нет.**

---

## Критические баги из full-audit-report.md (11 CRITICAL)

| # | Баг | Статус | Файл |
|---|-----|--------|------|
| C1 | `datetime.now()` при импорте | **НЕ исправлен** | `krm_models.py:44,64` — `default=datetime.now(timezone.utc)` |
| C2 | Prophet n_points NameError | **Исправлен** | `prophet_forecast.py:161` — `n_pts = len(model.history)` |
| C3 | `gap_metrics.record_analysis()` не существует | **НЕ исправлен** | `pipeline_steps.py:386` |
| C4 | SQL injection через f-string | **НЕ исправлен** | `fix_rpd_data.py:162` — `v['text']` в f-string |
| C5 | Захардкоженные креды БД в git | **НЕ исправлен** | `seed_users.py:10` — `postgres:700009` |
| C6 | XSS через dangerouslySetInnerHTML | Нужна проверка | `frontend/VacancyCard.tsx` |
| C7 | JWT декодит header вместо payload | Нужна проверка | `frontend/auth.tsx` |
| C8 | Spread options перезаписывает Authorization | Нужна проверка | `frontend/api.ts` |
| C9 | Дублирующий тест (никогда не запускается) | Нужна проверка | `test_ltr_recommendation_engine.py:234` |
| C10 | Дублирующий тест | Нужна проверка | `test_ltr_recommendation_engine.py:603` |
| C11 | `asyncio.run()` + `get_pool()` = None | **НЕ исправлен** | `runner.py:288-296` |

**Итого: 1 из 11 исправлен, 6 не исправлены, 4 требуют проверки.**

---

## HIGH-баги — выборочные проверки

| # | Баг | Статус | Доказательство |
|---|-----|--------|----------------|
| H1 | SkillMetrics category всегда WRONG | **НЕ исправлен** | `market_metrics.py:58` — `__post_init__` проверяет `if not self.category`, но `category` задаётся до setattr |
| H9 | None webhook secret = True bypass | **НЕ исправлен** | `webhooks.py:45-47` — `if expected is None: return True` |
| H35 | `dependencies = []` в pyproject.toml | **НЕ исправлен** | `pyproject.toml:10` — пустой список |

---

## Что реально изменилось

1. **Удалён мёртвый `self.gap_analyzer`** из `recommendation_engine.py`
2. **Добавлен cap `min(avg_gap, 1.0)`** в readiness score
3. **Исправлен `cluster_relevance`** — дефолт теперь `0.1 + 0.4 * (freq/max_weight)`
4. **Исправлен `n_points`** в Prophet fallback
5. **READINESS_DOMAIN_WEIGHT** обнулён (был -0.15, стал 0.0) — убрана двойная корневая ошибка, но веса не пересчитаны по плану

---

## Вывод

Проект содержит 208 задокументированных находок. Применены 4 из них (3%).

### Нужно сделать для реального улучшения:

**Приоритет 1 — Критические баги (C1-C11):**
1. Исправить `datetime.now()` в `krm_models.py` — использовать `default_factory`
2. Исправить SQL injection в `fix_rpd_data.py` — параметризованные запросы
3. Убрать hardcoded creds из `seed_users.py`
4. Исправить `gap_metrics.record_analysis()` в `pipeline_steps.py`
5. Исправить `asyncio.run()` + `get_pool()` в `runner.py`
6. Проверить и исправить XSS/JWT/spread в frontend

**Приоритет 2 — Readiness formula (Fixes 2-4):**
1. Применить redesigned weights: `0.45/0.30/-0.25`, удалить `READINESS_DOMAIN_WEIGHT`
2. Переписать формулу в `profile_evaluator.py:266-278`
3. Добавить `readiness_weights` параметр в конструктор

**Приоритет 3 — HIGH-баги:**
1. Исправить SkillMetrics `__post_init__` порядок
2. Исправить webhook secret bypass
3. Заполнить `dependencies = []`

---

## Верификация после фиксов

```bash
ruff check src/
mypy src/ --strict
pytest tests/ -v
```
